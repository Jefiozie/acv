import { Component } from '@angular/core';

/** Placeholder — will be replaced in plan 03-02. */
@Component({
  standalone: true,
  selector: 'app-subscribe',
  template: '<p>Subscribe (plan 03-02)</p>',
})
export class SubscribeComponent {}
